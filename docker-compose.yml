services:
  ollama:
    image: ollama/ollama
    container_name: ollama
    ports:
      - "11434:11434"

  agent-api:
    build: .
    container_name: agent-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      - ollama
    volumes:
      - .:/app
